defaults:
  - model: ouro_2_6b_r3
  - agent: default
  - search: e5_wiki18
  - eval: default
  - _self_

hydra:
  run:
    dir: outputs/logs/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: false
